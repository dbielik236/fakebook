<nav class="navbar navbar-expand-lg bg-primary navbar-dark">
  <div class="container-fluid">
    <%= link_to 'FakeBook', root_path, class:"navbar-brand" %>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Friends', users_path, class:"btn btn-link nav-link" %>
          </li>
          
              <li class="nav-item">
                <%= link_to 'Profile', user_path(current_user.id), class:"btn btn-link nav-link" %>
              </li>
              <li class="nav-item text-center">
                <%= button_to("Log Out", destroy_user_session_path, class:"btn btn-link nav-link", method: :delete) %>
              </li>
           
        <% else %>
          <li class="nav-item">
            <%= link_to 'Log In', new_user_session_path, class:"btn btn-link nav-link " %>
          </li>
  
           <li class="nav-item">
            <%= link_to 'Sign Up', new_user_registration_path, class:"btn btn-link nav-link "%>
          </li>
        <% end %>


      </ul>
    
    </div>
  </div>
</nav>