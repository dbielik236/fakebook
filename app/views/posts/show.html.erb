<div class="card shadow-sm mx-auto mb-3" style="max-width:600px">
  <div class="card-header">
    <strong><%= link_to (@post.user.first_name + " " + @post.user.last_name), user_path(@post.user.id)%></strong>
    <br>
    <%= @post.created_at.in_time_zone("America/New_York").strftime("%B %d, %Y at %l:%M %P %Z") %>
  </div>
  <div class="card-body">
    <p class="card-text"><%= @post.body %></p>
    <%= render 'comments/comment', post: @post %>
    <%= render 'comments/form', comment: @post.comments.build, submit: 'Comment' %>
  </div>
</div>  
<div class="card shadow-sm mx-auto mb-3" style="max-width:600px">
  <div class="card-body">
  <%= link_to "Back to posts", posts_path , class: "btn btn-secondary" %>
  </div>
</div>

